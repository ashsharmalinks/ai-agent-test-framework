[pytest]
addopts = -q --alluredir=./allure-results
testpaths = tests
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    perf: performance tests
    evals: dataset-based evaluation tests
